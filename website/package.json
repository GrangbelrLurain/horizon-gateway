{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "generate:og": "node scripts/generate-og-image.mjs",
    "build": "pnpm run generate:og && astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist --project-name=horizon-gateway --branch=main --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^4.118.0"
  }
}